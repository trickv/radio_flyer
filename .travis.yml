language: python

python:
  - "2.7"
  - "3.5"
  - "3.6"

install:
  - cat requirements.pip | grep -v ^picamera$ | pip install -r /dev/stdin

script:
  - pylint --disable superfluous-parens --disable missing-docstring *.py */*.py
